query GetAllPlayerStats($groupId: UUID!, $season: Int!) {
  seasonStats(condition: { groupId: $groupId, season: $season }) {
    player {
      id
      fullName
    }
    games
    plateAppearances
    atBats
    hits
    doubles
    triples
    homeruns
    xbh
    walks
    strikeouts
    sacFlies
    gidp
    runs
    rbi
    battingAverage
    onBasePct
    sluggingPct
    ops
  }
}
