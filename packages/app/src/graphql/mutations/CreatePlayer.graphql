mutation CreatePlayer($id: UUID, $firstName: String!, $lastName: String) {
  createPlayer(input: { player: { id: $id, firstName: $firstName, lastName: $lastName } }) {
    player {
      id
      firstName
      lastName
    }
  }
}
